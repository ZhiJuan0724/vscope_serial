# 已知问题列表
**问题最前面的从0-3数字越小任务越紧急**
**问题修复后提交前先标记任务完成**
---

## 已完成
 - [x] 0.在绘制阶跃类的图时 (例如 0 0 0 100 200 100 0) 0到100、100到200、200到100、100到0的连线 在缩小到比较小时可能会出现连线消失、漂移等情况
 - [x] 0.点击绘图停止时不是立刻停止 如果处理时间比较长 考虑先把UI更新完并再完全停止前不响应用户点击事件
 - [x] 0.如果外部串口已经断开(例如用户拔掉了串口的连接线) 此时发送失败后提示串口已断开连接 并且应用内的连接要正确更新为断开
 - [x] 0.如果数据全为0点击自适应y轴这整个界面会乱掉 如果全为0则不进行自适应 同理x轴如果只有3以内则不进行自适应
 - [x] 1.单通道放大缩小y最大只能放大到整个y轴140长，改成能0-1就占据一个屏幕的y轴

## 未完成
### 紧急 (1)
 - [x] 1.如果绘图缩小到一定程度且开启绘制连线时 则考虑不需要再绘制点 已免和线重合导致线的宽度变得很宽
 - [x] 1.对于只有整数型数据的绘图 y轴不管怎么缩放都不要出现小数的刻度线,但是10.0k这种可以 光标、x-x、y-y等功能都尽量只显示整数
 - [x] 1.添加图例功能 显示一个悬浮窗显示各通道名称 这个功能改了之后 偏移功能的标签就不用显示通道名称 只显示通道号如Ch0这种

### 中等 (2)
 - [ ] 2.把通道设置的偏置功能放到通道列表的显示勾选框左侧 也是勾选框
 - [ ] 2.绘图界面发送和接收的数据在原始数据界面能看到 但是在绘图界面发送的是HEX格式时在接收数据窗口要正确显示HEX格式而不是解析成文本导致乱码显示
 - [ ] 2.现在不需要限制绘图时不能切换窗口 绘图是也能开启原始数据界面的接收 同时即能查看图形也能看到原始数据
 - [ ] 2.上一轮绘图点击结束绘图后再开始需要保持上一轮缩放 除非重新启动软件绘图的缩放才重置
 - [ ] 2.众邦电控 支持八通道协议 在解析器配置中可修改四通道或是八通道 八通道的区别就是启动绘图发送的地址为8个接收的数据也为8个校验均与4通道保持一致
 - [ ] 2.使用光标功能时通道名称较长叠加测量的y值较长时文本会突出背景框
 - [ ] 2.进行自适应时如果通道开启了偏置 则该通道的自适应在自己的y轴缩放进行自适应

### 低优先级 (3)
 - [ ] 3.众邦协议的配置窗口输入地址只允许输入十六进制 键入其他无效
 - [ ] 3.通道设置界面确认按钮旁边添加重置按钮 一键恢复通道设置
 - [ ] 3.绘图界面的导入导出需要能选择是csv还是bin bin可以在前面加文件头标识然后加上长度信息最后加上校验信息 记得原始数据界面导出的原始数据不要加任何东西 只是绘图界面的导入导出进行修改
 - [ ] 3.快捷选取通道时由于名称长度变化自动调整通道列表的宽度以全部显示所有控件
 - [ ] 3.如果快捷选取的通道地址高位两位都是0则十六进制地址只显示后2字节 避免占用过多通道面板宽度
 - [ ] 3.需要支持vofa的JustFloat协议 https://www.vofa.plus/plugin_detail/?name=justfloat
