# 已完成的TODO列表

## 已完成
 - [x] 1.有时候外部串口断开但状态没有更新 在如下几个操作自动检测串口连接状态1、点开串口连接的窗口2、点击开始绘图 在检测到连接断开后自动刷新串口数据如果该串口还在则尝试自动重连1次 如果不在则更新状态为断开而非用户手动断开
 - [x] 0.在使用random数据测试时 我发现如果放大后绘图会十分卡顿 停止实时绘图也卡顿 单通道下也卡顿 甚至包数都没超1W包 但是使用sine测试时就没这个问题 是不是跟数据随机性太大LOD处理有关
 - [x] 0.在绘制阶跃类的图时 (例如 0 0 0 100 200 100 0) 0到100、100到200、200到100、100到0的连线 在缩小到比较小时可能会出现连线消失、漂移等情况
 - [x] 0.点击绘图停止时不是立刻停止 如果处理时间比较长 考虑先把UI更新完并再完全停止前不响应用户点击事件
 - [x] 0.如果外部串口已经断开(例如用户拔掉了串口的连接线) 此时发送失败后提示串口已断开连接 并且应用内的连接要正确更新为断开
 - [x] 0.如果数据全为0点击自适应y轴这整个界面会乱掉 如果全为0则不进行自适应 同理x轴如果只有3以内则不进行自适应
 - [x] 1.单通道放大缩小y最大只能放大到整个y轴140长，改成能0-1就占据一个屏幕的y轴
 - [x] 1.如果绘图缩小到一定程度且开启绘制连线时 则考虑不需要再绘制点 已免和线重合导致线的宽度变得很宽
 - [x] 1.对于只有整数型数据的绘图 y轴不管怎么缩放都不要出现小数的刻度线,但是10.0k这种可以 光标、x-x、y-y等功能都尽量只显示整数
 - [x] 1.添加图例功能 显示一个悬浮窗显示各通道名称 这个功能改了之后 偏移功能的标签就不用显示通道名称 只显示通道号如Ch0这种
 - [x] 1.默认和偏置的y轴的刻度值不能缩写成K
 - [x] 1.观察功能的光标鼠标不太能够拖动
 - [x] 1.针对导入导出的数据 如果是使用众邦协议的在内容中包含配置通道地址和通道名称信息 在导入时自动填入 如果是其他协议则只需要保留通道名称信息
 - [x] 1.完成类似zobow_device.py的对于justfloat的工具 接收的协议是'r [通道1] [通道2] \n'结尾 根据发送通道的地址自动识别通道数量
 - [x] 1.JustFloat协议在自动识别后开启部分通道的偏置功能 重新自动识别如果之前开启的通道这次识别到未开启 但是偏置仍会显示在绘图上
 - [x] 1.添加数据收发界面一个选项 勾选后点击发送不自动清空用户发送区的内容
 - [x] 1.常用快捷选择的波特率9600之前的去掉 添加512000 1152000
 - [x] 1.用户手动输入的其他波特率反馈有时候重启软件没有记录
 - [x] 1.在通道设置界面多次点击取消软件发送了崩溃闪退
 - [x] 1.JustFloat默认通道数是0 也就是 自动识别 除非用户手动修改 如果用户手动修改也需要持久化保存
 - [x] 1.观察功能的光标在添加时有时会出现在屏幕显示的范围外侧 需要缩小或者拖动x轴才能找到
 - [x] 2.使用光标功能时通道名称较长叠加测量的y值较长时文本会突出背景框
 - [x] 2.增加'添加观察'功能 观察功能类似光标 但是不跟随鼠标 每次点击添加一根线 这种光标的上侧有操作柄可以拖动 然后形似光标功能 显示此时光标所在x的所有通道的y值 也是跟光标一样半透明窗口显示 然后可以通过右键操作柄选择删除这个观察
 - [x] 2.把通道设置的偏置功能放到通道列表的显示勾选框左侧 也是勾选框
 - [x] 2.上一轮绘图点击结束绘图后再开始需要保持上一轮缩放 除非重新启动软件绘图的缩放才重置
 - [x] 2.众邦电控 支持八通道协议 在解析器配置中可修改四通道或是八通道 八通道的区别就是启动绘图发送的地址为8个接收的数据也为8个校验均与4通道保持一致
 - [x] 2.进行自适应时如果通道开启了偏置 则该通道的自适应在自己的y轴缩放进行自适应
 - [x] 2.修改日志 现在如果接收频率很高的话反复发送日志比较占用性能 改成如果检测到频率很高就降低日志的频率 比如收到50包统一发送日志 这个数值可以调整
 - [x] 2.修改zobow_device.py使其适配八通道测试要求
 - [x] 2.图例直接显示通道名称 不需要显示"Chx 名称"
 - [x] 3.众邦协议的配置窗口输入地址只允许输入十六进制 键入其他无效
 - [x] 3.通道设置界面确认按钮旁边添加重置按钮 一键恢复通道设置
 - [x] 3.绘图界面的导入导出需要能选择是csv还是bin bin可以在前面加文件头标识然后加上长度信息最后加上校验信息 记得原始数据界面导出的原始数据不要加任何东西 只是绘图界面的导入导出进行修改
 - [x] 3.如果快捷选取的通道地址高位两位都是0则十六进制地址只显示后2字节 避免占用过多通道面板宽度
 - [x] 3.需要支持vofa的JustFloat协议 https://www.vofa.plus/plugin_detail/?name=justfloat
