# Changelog

<!--
维护说明：
- 所有值得用户关注的变更都记录在这里。
- 提交涉及发布内容时，提交前必须让用户核对本文件对应版本段落。
- 版本 tag 推送后产生的新变更应记录到下一版本，发布日期在发布前填写。
-->

## v1.0.7-beta.6 - 2026-08-01

### Added
- 数据收发新增 TCP 客户端、单客户端 TCP 服务端和固定远端 UDP；串口绘图新增 TCP 客户端和 UDP，网络入口可在应用高级设置中启用。
- 主页面支持动态添加、右键关闭和拖动排序；页面顺序仅记录当前开启页面，重新打开的页面追加到末尾。
- 高级设置新增“按页面独立保存串口参数”；默认仍由数据收发、Shell 和绘图共用原有全局串口配置。

### Changed
- 串口、TCP 和 UDP 统一使用单连接所有权和双向数据传输流程；连接或正在收发时禁止关闭页面及进入不兼容页面。
- 统一数据连接与探针连接架构，修正网络和探针相关提示，并提高连接、重连与退出生命周期的一致性。
- 主标签的添加和右键菜单改为无慢动画的自绘控件；新增按钮紧跟最后一个页面，页面全部显示时自动隐藏。
- 各设置窗口只在左侧不同分类之间显示分割线；页面标签增加未选中底色，并取消切换时的矩形过渡闪现。

### Fixed
- 修复快速展开多条发送时，扩展内容先挤入旧窗口再跳到新区域的闪现；现在会等待窗口拉伸完成后再显示面板。
- 修复网络开关、连接类型与底部状态文案不同步的问题；启用网络后，未连接状态会明确显示“串口/网络未连接”。
- 修复 TCP 服务端客户端断开后监听被一并关闭、多客户端未正确拒绝，以及网络发送异常时连接状态清理不完整的问题。

## v1.0.7-beta.5 - 2026-07-30

### Added
- 应用高级设置新增调试模式；开启后日志历史会记录串口连接与底层 I/O 的详细诊断信息，便于排查特定电脑上的串口异常。
- Windows 原生崩溃时自动保存小型转储和异常元数据，下次启动会提示并可打开转储目录；高级设置可关闭此功能，并提供可检查缺失或不匹配符号的本地报告脚本。
- RTT Viewer 新增开始/停止、SEGGER 虚拟终端 0～15、All Terminals 汇总、RTT Down 0 发送、暂停、导出及终端名称和颜色配置。
- 新增“探针绘图”页面，支持运行态 HSS 采样、RTT Up 通道、ELF/AXF/OUT 变量选择和 J-Scope 格式识别，并提供跟随、缩放、光标、观察、Delta X/Y、图例、实时值及独立的绘图性能和外观设置。
- Windows 发布包内置经过 SHA-256 校验的 xPack OpenOCD 0.12.0-7 轻量运行时、完整目标脚本和许可证，不再要求普通用户单独安装 OpenOCD。
- 新增可选的外置 pyOCD Python API 后端，支持 CMSIS-DAP 的 RTT Viewer、RTT 绘图、通道识别和 HSS；高级设置可指定 Python 环境并查看 Python/pyOCD 版本，连接时可选择自动、仅 v1 或仅 v2 枚举。
- 主窗口新增连接快捷键：F1 打开当前页面对应的连接配置，F2 快捷连接，F3 快捷断开，F5 依次断开并重连；可在高级设置中统一关闭。

### Changed
- 主窗口标签改为可横向滚动并支持长按拖动排序，鼠标滚轮可横向浏览，选中标签使用向内容区展开的圆角过渡。
- 串口绘图支持触控板双指平移和捏合缩放；纯缩放保持跟随状态，发生平移时才退出跟随。
- 探针连接支持自动、外部 J-Link、外置 OpenOCD、内置 OpenOCD 和外置 pyOCD；CMSIS-DAP 自动优先使用外置 OpenOCD，其次为内置 OpenOCD 和外置 pyOCD。
- 探针连接与 RTT/HSS 数据活动显式分离；RTT Viewer 和探针绘图分别保存控制块定位及轮询间隔，停止接收时保持非侵入式空闲连接。
- RTT 与 Shell 统一终端字体和设置样式；探针列表改为手动刷新并显示 USB 设备名称，目标芯片支持模糊检索。
- 探针绘图与串口绘图统一交互和工具图标，通道按实际识别数量显示并支持折叠，大范围绘制使用质量优先 LOD 策略。
- 通道名称输入上限统一提高到 `64` 个字符；绘图图例浮窗会根据最长可见名称自适应宽度，优先完整显示通道名称。
- 应用高级设置新增统一的内存上限视图，可调整绘图历史预算，并查看绘图、数据收发、Shell 和 YMODEM 的当前占用与限制。
- “保持绘图”仅支持同一接收协议且通道数一致的新数据流继续追加；切换协议、自动识别通道数变化或导入数据后会清空旧历史。
- 应用高级设置新增可选“绘图高频接收合并”；开启后仅在绘图接收期间短暂合并连续小块串口数据，降低高频大量数据的处理开销。
- Delta X/Y 测量按钮支持右键分别配置两条测量线的颜色和不透明度，Delta Y 可额外控制拖动时是否吸附到波形。
- 普通通道和数学通道的线宽、点半径改为数值输入；通道编辑窗口新增与列表共用的偏置开关，开启后可直接编辑当前通道的 offset 和 scale。
- 大范围绘图选择“质量优先”时，LOD 改用趋势线与极值包络绘制，避免单点突变被连续连线扩展成三角形；均衡和性能优先保持原有绘制方式。
- 应用主配置 `settings.json` 改为带缩进和换行的易读格式，并按全局、串口、数据收发、Shell、串口绘图、探针绘图、RTT 等功能及其子项分组；旧版单层配置会自动迁移。地址配置及多条发送配置继续使用相同的格式化 JSON 风格。

### Fixed
- 修复部分 Windows 电脑首次连接串口时因原生 FFI 延迟初始化竞态导致应用直接退出的问题；应用启动阶段会先执行一次无副作用的串口绑定预热。
- 探针监控强制使用非侵入式安全路径，RTT Viewer、RTT 绘图和 HSS 不执行停核、复位、恢复、烧录、擦除或核心调试操作。
- 完善探针枚举、连接、启停和中途拔出处理；修复 RTT Viewer 终端显示、RTT 通道解析、HSS 横轴和探针绘图交互等稳定性问题。
- 高频探针数据不再写入普通日志；调试模式下仅保留限频汇总，避免日志影响数据接收和界面响应。
- 修复取消更新包下载后仍会继续重试或切换下载来源，导致界面一直停留在“正在取消下载”的问题。
- 修复跨稳定版与 Beta 版更新时回退槽选择错误的问题；升级前的稳定版和 Beta 版现在始终分别保存到各自槽位。
- 修复地址配置多项搜索结果跳转错位及名称删空后焦点跳到地址栏的问题；Zobow 地址失焦后统一补全为八位十六进制，r 协议继续保留十进制或十六进制原文。
- 探针连接过程中现在可以直接取消并完整清理后端资源；探针绘图高频接收减少无关界面重建，改善持续采样时的响应。

## v1.0.7-beta.4 - 2026-07-20

### Added
- 数据收发新增独立“自动换行”功能，默认在相邻数据包空闲超过 `100 ms` 时换行；时间戳、自动换行和 HEX 显示可分别控制。
- 绘图新增性能优先、均衡和质量优先三档大范围绘图质量，并在状态栏显示实际渲染 FPS。
- 新增可选的独立 Shell 页面，支持命令行与逐键模式、独立编码和行尾、本地回显、命令历史、终端导出、字体预览及 YMODEM 文件传输。

### Changed
- 数据收发、Shell 和绘图在接收期间锁定当前页面；各类发送共用有序写入队列，Shell 与 YMODEM 改用流式处理。
- 三个业务页面统一工具栏、状态、背景和设置入口；设置改为左侧分类导航、右侧连续内容并支持滚动联动，绘图随机源启用状态改用绿色标识。
- 重组绘图设置分类与顺序，Shell 字号改为数值输入并支持即时预览。
- 绘图历史内存上限改为 `1~8 GiB` 可配置，默认保持 `2 GiB`。
- 应用信息与设置拆分为独立入口，重排版本更新区域，恢复默认设置改用随机四位验证码确认。
- 主窗口默认宽度调整为 `950px`、最小宽度统一为 `800px`，多条发送扩展面板同步遵循窗口尺寸限制。
- 绘图接收协议默认选择“众邦电控”，并继续仅在该接收协议下使用“众邦内置”发送协议。

### Fixed
- 修复数据收发与多条发送的 HEX 输入、按钮状态、循环停止、手动发送限制、时间戳累积显示及布局问题。
- 限制 FireWater 与 JustFloat 异常数据的解析残留；原始文本跨包接收中文不再乱码，运行中调整随机源频率不再清空绘图历史。
- 优化 Windows 串口高波特率读取缓冲与回调；修复长时间运行后的时间换算风险，并为串口写入增加异常退出和超时保护。
- 应用设置和地址配置改为原子保存并保留备份；文件损坏时自动恢复有效备份并提示，高频设置修改会合并写入且退出前确保落盘。
- CSV、BIN 和旧版 DAT 改为预检后流式导入，避免大文件产生整份内存副本；绘图及原始数据导出改为事务写入，取消或失败时保留既有文件。
- Shell 接收积压和 YMODEM 输入增加固定内存上限与过载提示；YMODEM 接收完成前仅保留临时文件，取消、断线或校验失败不会留下不完整文件。
- 绘图局部视口改为预取精确数据块；定位条支持整轨抓取、LOD 预览、防抖换窗和两端可视留白，避免窗口块难以拖动、边框被裁切、反复加载、拖到空白，以及曲线或悬停光标越出绘图区。
- 绘图状态栏移除瞬时加载提示和重复的光标详情，长信息超出可用宽度时改为单行省略显示。
- 修复关闭跟随后回看历史数据时，实时值浮窗不再更新最新接收值的问题。
- 临时关闭 Windows 应用语义树，规避 Flutter Tooltip 触发 AXTree 更新失败后错误日志持续刷屏的问题。
- 串口刷新后若上次选择的端口已不存在，连接弹窗会明确标注该端口不可用并禁用连接，避免历史选择被误认为当前设备。
- 修复 Shell 清屏、光标位置与样式、输入焦点、文本边界、选择错位、快速回显顺序及 `CRLF` 识别问题。

## v1.0.7-beta.3 - 2026-07-14

### Added
- 数据收发页新增多条发送面板：可建立多个独立配置，逐条保存文本或 HEX 内容、启用状态和间隔，并支持单条发送、执行一轮、持续循环以及 JSON 导入导出。
- 触发功能支持已启用且可见的无偏移数学通道，按表达式原始值判定，触发观察会同步记录数学通道值；包含 `CHn[...]` 数据偏移的数学通道暂不支持触发。
- 绘图 CSV/BIN 导出可在同一窗口选择数据范围和最多 16 个普通通道加 4 个数学通道；CSV 只写入所选数据，数学列表头使用原表达式，BIN 继续保留通道名称和观察等附加信息。数学数据的 `NaN` 可原样导出并重新导入。

### Changed
- 主页面选中标签与内容区改用一致背景色，绘图页两层工具栏、通道表头和通道列表统一使用页面表面色。
- 绘图区没有数据时固定显示白色空态，收到首个有效数据后再应用用户选择的暗色或亮色背景。
- 绘图 X 轴和垂直网格改为按绝对数据值动态生成规则刻度，缩放和平移时优先使用 `1/2/5 × 10ⁿ` 间距并自动调整格子数量，避免出现混乱刻度值。
- 主 Y 轴和偏置 Y 轴优先使用整数规则刻度；偏置轴会按通道原始值域生成刻度，避免缩放、偏置或浮点协议产生难以读取的零碎小数。
- 左侧 Y 轴会根据当前刻度文本和绘图字体大小自动调整边距，避免大字号或较长数值溢出到通道列表。
- 绘图文字在用户选择的字号增量基础上统一增加 `1px`，高级设置的字体大小位置新增加粗开关并持久化保存。
- 主窗口顶部导航改为标签样式，并会记住退出前停留的页面；协议页面入口暂时隐藏。

### Fixed
- 修复大数据绘图反复缩放后，精确窗口暂时为空时所有曲线可能消失的问题；此时会继续使用全量 LOD 数据绘制。
- 修复开启“保持绘图”后跨接收协议保留不兼容历史可能造成绘图下标错位的问题；开始新一轮绘图时会自动清理不兼容历史。

## v1.0.7-beta.2 - 2026-07-12

### Added
- 绘图新增定位条，可在高级设置中显示工具栏入口；定位条支持查看当前窗口、拖动或点击快速定位。
- 通道地址选择窗口会记住上次搜索条件和列表位置，便于连续为多个通道选择地址；配置编辑窗口新增通道后会自动定位到列表底部。
- 观察点支持锁定；触发新增的观察默认锁定，锁定后绘图区不能右键删除或拖动，管理窗口仍可切换锁定状态并删除。
- BIN 绘图导出会携带观察点位置、备注和锁定状态，重新导入 BIN 后可恢复；CSV 仍只导出数据内容。
- 绘图高级设置新增“保持绘图”，开启后停止绘图再开始会在原有绘图数据后继续追加。

### Changed
- Zobow 和 r 协议从地址配置选择通道后，重启应用会恢复配置带入的通道名称；手动改到其他地址会清空该名称。

### Fixed
- 修复 Shell 终端接收内容固定按 UTF-8 解码的问题，现与数据收发文本编码设置保持一致。
- 修复导出 CSV/BIN 时删除文件扩展名会导致后续难以导入的问题；导出会自动补全扩展名，导入支持选择无扩展名文件。
- 修复 BIN 导出后重新导入时 r 协议通道地址未恢复的问题。
- 修复 Shell 逐键模式下方向键和 Tab 无法正确发送的问题。
- 修复观察点在当前窗口外时可能被吸附到旧可见点的问题，观察跳转仍可移动到正确 X 位置。
- 修复触发配置通道列表未按当前协议实际普通通道数过滤的问题，Zobow 仅显示当前 4 或 8 个通道。

## v1.0.7-beta.1 - 2026-07-08

### Added
- 绘图新增触发工具，可配置启用状态、普通通道条件、比较方式、累计命中次数、触发次数、触发后行为和观察标记。
- 触发条件支持大于、小于、等于、上升沿经过和下降沿经过；通道选择只包含当前打开的普通通道。
- 观察功能支持备注、管理弹窗、跳转和删除；观察总数限制为 100 条，达到上限后不再新增。
- 光标按钮支持有数据时右键输入整数 X 索引并直接跳转到对应位置，跳转前会校验范围。
- 绘图 CSV/BIN 导出支持选择起始点和结束点，导出文件内 X 从 0 重新编号，并支持导出中取消。
- Zobow/r 地址配置编辑窗口支持按名称或地址搜索，点击搜索结果可跳转到原列表位置继续调整顺序。
- 绘图图例和实时值浮窗拖动后会保存位置，下次打开应用时保持上次位置。

### Changed
- 触发入口默认隐藏，可在高级设置中开启；开启触发模式后工具栏按钮以绿色填充高亮。
- 状态栏包速率显示改为更稳定的统计窗口，降低大包或高频接收时的短时波动。
- 高频绘图场景优化解析、LOD 更新和窗口刷新策略，降低大数据量下的界面压力。
- 优化大数据绘图 BIN 导出吞吐，导出进度弹窗显示估算写入速度。
- 高级设置中的绘图工具相关选项顺序调整，使常用开关更集中。
- 触发、光标和观察等带右键功能的工具按钮补充左键/右键操作提示。

### Fixed
- 修复多实例同时运行时共享 `latest.log` 可能互相重命名、截断或占用的问题，日志改为每实例独立文件。
- 更新和回退流程新增多实例保护，检测到其他实例时提示用户先保存数据，并在确认后请求关闭其他实例再继续。
- Windows 更新器增加同名更新锁和目标程序运行检查，避免多个更新任务或更新器并发替换文件。
- 固定 Windows Flutter 生成插件注册文件的换行规则，减少无实际内容修改却反复显示变更的问题。

## v1.0.6 - 2026-07-03

### Added
- 新增 Shell 终端和 YMODEM 文件传输，支持 ANSI 显示、命令行及逐键输入。
- 新增数学通道，支持四则运算、`abs()` 和按 X 偏移取值。
- 绘图新增实时值、点击定位观察、亮色/暗色背景和偏置绑定。
- 自动更新新增稳定版/Beta 通道、GitHub/Gitee 来源及本地版本回退。
- 完善 Zobow/r 地址配置的导入、导出和编辑能力。

### Changed
- 应用展示名称改为 `SerialTools`，并更新 Windows 应用图标。
- 优化高频和百万级数据绘图性能，随机源最高支持 100000 包/s。
- 统一通道右键操作、显示图标、颜色设置及配置持久化。
- 原始数据默认显示上限提高到 100000 行，收发文本统一使用所选编码。
- 文本和 BIN 支持完整数据导出、目录选择及进度显示。
- 优化绘图工具栏、网格、浮窗、测量和配置界面，并加强运行期间的误操作保护。

### Fixed
- 修复部分 FT232 等 USB 串口设备枚举缓慢并阻塞界面的问题。
- 修复原始数据长时间接收、自动滚动和无换行文本可能引起的卡顿。
- 修复 Zobow、r、固定帧和 JustFloat 的多项解析及配置问题。
- 修复数学通道、高频绘图、重绘和测量高亮的多项异常。
- 修复通道右键菜单、弹窗状态和 Beta 更新下载回退问题。

## v1.0.6-beta.9 - 2026-07-02

### Added
- 绘图工具栏新增“实时值”开关，可在绘图区浮窗中查看各通道最新值。
- 绘图高级设置新增暗色/亮色背景切换和悬浮窗透明度调节。
- 观察功能新增“点击定位观察”模式，可先让观察线跟随鼠标，再通过左键固定位置。
- 通道列表空白处新增“重置全部通道”入口，可重置当前通道地址、显示、颜色、偏置、数据类型等通道设置，并保留已保存配置文件。

### Changed
- 应用展示名称改为 `SerialTools`，并更新 Windows 应用图标。
- 众邦电控通道默认数据类型改为 `int16`。
- 通道列表显示/隐藏开关改为眼睛图标，便于直接识别当前通道是否显示。
- 通道颜色预设、绘图区浮窗和测量手柄随绘图背景切换，提升亮色/暗色背景下的辨识度。
- 通道长名称支持悬停查看完整内容。
- R 协议地址输入框默认更窄，并会随输入内容自动变宽。
- 快捷选择地址弹窗和地址配置编辑界面加宽，便于查看较长名称和地址。
- R/Zobow 地址在绘图运行时锁定，停止后才能编辑或应用地址预设，通道名称仍可随时修改。

### Fixed
- 修复众邦通道地址、众邦通道数据类型和固定帧逐通道数据类型重启后不能恢复的问题。
- 修复绘图背景网格线与 X/Y 坐标轴刻度值不对齐的问题。
- 修复普通/密集网格密度显示差异不明显的问题。
- 修复 r 协议配置编辑和 CSV 导入后无法保留十进制地址格式的问题，并拒绝不符合十进制或 `0x` 十六进制规则的地址。
- 修复 JustFloat 配合 r 协议选择地址预设后，通道名称和地址不能立即刷新的问题。
- 修复编辑地址配置保存后，快捷选择地址仍显示旧预设列表的问题。

## v1.0.6-beta.8 - 2026-06-29

### Added
- 数学通道表达式支持 `CHn[偏移]` 按 X 方向取相邻点数据，无法计算的点继续按空心点显示。
- 众邦/R 协议配置支持导出已有配置；r协议新增“宽松通道设置”，可自动压紧留空的地址槽位。
- 发送协议增加配置入口，仅在当前协议有可配置项时可用。

### Changed
- C 代码导入地址配置兼容 `VisualScope` 函数名和 `TxVar.VsCh[i]` 输出左值，并减少单函数、单赋值场景下的手动匹配要求。
- 随机源频率改为整数 Hz，最大支持 100000 Hz；高频接收会自动降低运行时刷新压力，改善 100K 包/s 场景下的界面响应。
- 数学通道表达式输入框增加合法性图标；通道颜色支持调色盘、RGB 和 HEX 输入。
- 通道高级设置入口移动到右键菜单，数学通道删除也统一通过右键菜单完成。
- 绘图界面优化重建和分层绘制，降低高频接收、拖动、光标和数学通道 offset 操作时的卡顿。
- 偏置通道交互优化：右侧偏置 Y 轴可上下拖动调整 offset；Shift+拖动会按首次移动方向锁定 X/Y 缩放；X 轴缩小到显示上限后不再继续移动视口。
- 数据收发文本导出改为从完整原始接收数据按当前编码重新解析，文本和 BIN 大文件导出均显示进度，减少界面无反馈。
- 增加绘图、协议、数据收发和 Shell/YMODEM 等关键用户操作日志，便于远程定位问题。

### Fixed
- 修复 C 代码导入时多赋值 case 可能误识别的问题。
- 修复通道列表滚动条遮挡控件、右键菜单状态异常和弹窗切换时可能红屏的问题。
- 修复高频随机源速率显示不准、串口源高频刷新批量过小、数学通道尾部补算和 offset 拖动卡顿等绘图性能问题。
- 修复停止绘图后修改通道样式、网格密度或偏置轴宽度时绘图区可能不立即刷新的问题。
- 修复 JustFloat + r协议初始化失败提示不清晰的问题；r协议地址 `0` 会按有效地址发送，仅空地址表示不发送。
- 修复 Zobow/r 协议配置切换、新增或编辑后下拉框不能立即刷新的问题。
- 修复 Zobow 异常数据下日志过多、长时间阻塞后可能持续超时且无法自动恢复的问题。
- 修复原始数据收发达到显示行数上限后持续接收明显卡顿、关闭自动滚动后仍会滚动的问题。
- 修复重启应用后绘图自动连接未恢复已保存波特率的问题。
- 修复部分 FT232 等 USB 串口设备枚举可能耗时十几秒，导致连接窗口和开始绘图长时间无响应的问题。

## v1.0.6-beta.7 - 2026-06-26

### Added
- 绘图新增 `Math1`~`Math4` 数学通道，可使用 `CH0`~`CH15`、四则运算、括号和 `abs()` 实时计算显示；CSV/BIN 导出保存表达式元数据但不导出实际计算数据列。
- 通道列表右键菜单支持区分空白、普通通道和数学通道；空白处可添加数学通道，数学通道可右键删除，为后续通道功能扩展预留入口。
- 绘图高级设置新增吸附点高亮颜色模式，可选择跟随光标线颜色或跟随各自通道颜色。

### Changed
- 优化数学通道设置弹窗，颜色选择、弹窗宽度和自定义颜色入口与普通通道保持一致；通道预设颜色增加一排。
- 绘图高级设置中的跟随位置和 Y 自适应占比改为百分比输入，默认分别为 90% 和 80%。
- 测量统计入口改为“工具栏显示开关”，高级设置只控制是否显示工具栏入口，点击工具栏按钮后才启用统计。
- 通道高级弹窗移除重复的偏移显示设置，并在数学通道表达式输入框下方补充支持语法提示。
- 应用信息更新区域在更新通道下方新增更新来源选择，支持自动、GitHub、Gitee；自动模式保持 GitHub 优先并在失败后尝试 Gitee，锁定来源时检查和下载都不切换到其他来源。

### Fixed
- 修复数学通道在观察浮窗、X 测量吸附高亮和底部状态栏中显示不完整的问题。
- 修复 X 测量线位于当前视口外时，切换吸附点高亮颜色会在视口边缘绘制错误高亮点的问题。
- 修复数学通道或普通通道浮窗中可能显示 `25340.149999999998` 这类浮点精度尾巴的问题。
- 修复通道右键菜单在连续右键、点击其他控件或打开弹窗时状态不正确的问题，并为满 16 个普通通道时保留可右键添加数学通道的空白区域。
- 修复通道列表右侧拖动条贴住内容、遮挡正常显示的问题。
- 修复 Beta 更新从 GitHub 下载失败回退到 Gitee 时，下载阶段按稳定版校验 beta tag 导致提示 `release tag_name does not match channel` 的问题。

## v1.0.6-beta.6 - 2026-06-24

### Added
- 绘图工具栏接入重绘 SVG 图标，覆盖自适应、缩放、测量、导入、光标和跟随等常用工具，并在进入绘图界面前预加载图标资源，减少首次显示空白。
- 新增固定 UI 文本集中管理入口，应用信息、更新、众邦/R 协议配置、外部导入、绘图和数据收发等主要界面文案逐步收敛到统一管理文件，便于校对和后续多语言处理。
- 新增 Codex 项目规则文档和本地 hook，约定代码读写优先使用 `pwsh`，并避免自动提交、自动 push 和自动回滚等高风险操作。

### Changed
- 绘图工具栏更新 P0 级图标样式：自适应改为 Auto X/Auto Y/Auto Full，测量简化为 Delta X/Delta Y，缩放 X/Y 统一为放大镜加方向字母，导入、光标和跟随图标优化小尺寸辨识度。
- 绘图工具栏隐藏统计测量入口，统计功能移入高级设置并默认关闭；统计范围跟随统计测量开关联动，不再拆成两个独立开关。
- 绘图工具栏和页面切换区域的 SVG 图标统一通过 `AppIcon` 接口管理，后续替换图标时不需要分散修改各处控件。
- 绘图高级设置、通道编辑、解析器配置、数据收发高级设置、Shell 设置和文件传输弹窗中的单行输入框/下拉框统一尺寸和内边距，减少同一二级窗口内控件大小不一致的问题。
- 绘图高级设置新增跟随位置和 Y 自适应占比，二者范围均为 50%~95%，默认 90%；全自适应会先应用 X 自适应，再按该占比应用 Y 自适应。
- 绘图窗口上限和丢弃包数支持 `K`/`M` 后缀输入和显示，丢弃包数范围调整为 0~10K。
- 众邦协议通道地址输入框加宽并恢复等宽字体，支持完整显示带 `0x` 前缀的 32 位十六进制地址。

### Fixed
- 修复众邦协议通道地址输入可输入非法十六进制字符或超过 `0xFFFFFFFF` 的问题。
- 修复无数据状态下部分绘图工具按钮显示为灰色导致图标对比不一致的问题，点击时保持无操作。

## v1.0.6-beta.5 - 2026-06-20

### Added
- 数据收发页面新增 Shell 终端模式，支持 ANSI 终端显示、命令行发送、逐键发送、复制粘贴、清屏，以及浅色/深色主题、光标样式和系统等宽字体切换。
- Shell 模式支持 YMODEM 发送和接收文件，支持自动/128/1024 字节包长选择，接收文件保存到应用目录下的 `exports/ymodem/`。
- 新增 Shell/YMODEM 外部 Python 虚拟串口测试工具，支持终端交互和 YMODEM 文件收发验证。
- 数据收发接收区非 HEX 模式下支持选择文本解码方式：UTF-8（默认）、GBK、BIG5、Shift_JIS、EUC-KR、Latin-1、ASCII，编码选项位于高级设置弹窗内。
- 应用信息高级设置新增一键恢复默认设置功能，恢复前需要输入确认文本，并保留绘图配置功能保存的 JSON 配置文件。

### Changed
- 数据收发工具栏的开始接收和停止接收收敛为同一个状态按钮，并恢复按钮轻量按下反馈。
- 文本模式接收时单行超过 4096 字符自动强制换行，避免串口持续输出无换行数据导致界面卡死。
- 收窄应用内临时提示宽度，避免遮挡左下角串口连接状态。
- 完善 Shell 模式下 YMODEM 数据接收链路，传输期间二进制数据直接进入 YMODEM 状态机，不经过终端显示流。
- 优化 YMODEM 文件传输弹窗，完成提示保留在弹窗内，长文件名和传输进度分行展示，并显示本次传输平均速度。
- 高频串口发送日志自动合并显示，避免 YMODEM 等快速发送场景刷出大量逐包日志。
- 恢复默认设置后的提示统一遵循全局临时提示设置。

## v1.0.6-beta.4 - 2026-06-17

### Changed
- 发布 CI 在创建 GitHub Release 后同步创建 Gitee Release，并上传同一批 Windows 更新附件。
- Beta 更新检查和下载允许在 GitHub 失败后回退到同版本 Gitee Release。

## v1.0.6-beta.3 - 2026-06-17

### Changed
- 自动更新缓存、解压 payload 和回退槽改为保存到应用目录下的 `updates/`，不再写入用户目录。
- 新增手动下载最新版更新包的测试入口，便于验证真实 Release 下载链路且不触发安装。

### Fixed
- 修复 Beta 更新下载失败后仍尝试 Gitee 导致异常提示不正确的问题。
- 修复 GitHub Release 资产下载连接中断时缺少重试的问题。

## v1.0.6-beta.2 - 2026-06-17

### Changed
- 应用信息页的版本回退功能移入高级设置，并统一高级设置内的文字层级和按钮尺寸。
- 应用信息页和更新弹窗在内容较多时支持滚动，但不显示滚动条。

### Fixed
- 修复 Beta 版本日志无法在应用信息页正确展示的问题。
- 修复应用信息页内容过长时版本回退区域不可见的问题。
- 修复更新通道选择控件视觉样式不协调的问题。

## v1.0.6-beta.1 - 2026-06-17

### Added
- 绘图界面支持按住 Shift 左右拖动缩放 X 轴，向右拖动放大、向左拖动缩小，原有 Shift+滚轮缩放操作保持不变。
- 支持从 GitHub Release 下载并校验 Windows 更新包，下载完成后可重启应用自动覆盖安装。
- 自动更新下载失败时会尝试同版本 Gitee Release，安装失败会自动回滚原程序文件。
- 绘图高级设置新增丢弃包数，可在每次开始绘图时跳过前 N 个成功解析的数据包。
- 自动更新支持稳定版/Beta 更新通道，Beta 使用 vX.Y.Z-beta.N 版本规则并由用户手动切换。
- 自动更新界面新增稳定版和 Beta 回退槽，可各保留一个安装前版本用于手动回退。

### Changed
- 数据收发接收区默认显示上限由 500 行提高到 10000 行，并支持在高级设置中调整。
- 数据收发接收区改用单段可选择文本，改善跨行选择以及 `Ctrl+A`、`Ctrl+C` 复制体验。
- 底部状态栏仅在 FireWater 协议下显示随机源状态。

### Fixed
- 修复应用信息页面中三级标题未按粗体样式显示的问题。
- 修复关闭自动滚动后接收区仍可能继续滚动到底部的问题。
- 修复接收区选择到行尾时选区背景延伸到空白区域的问题。
- 修复固定帧帧头/帧尾字节输入不便和通道类型下拉框尺寸不一致的问题。
- 修复 JustFloat 自动识别配合 r 协议时运行中仍显示多余通道地址槽位的问题。

## v1.0.5 - 2026-06-06

### Added
- 地址配置支持 CSV 导入，CSV 两列分别为通道名称和通道地址，并自动识别首行是否为表头。
- 地址配置选择支持搜索。
- 开启最新点跟随后拖动画布自动关闭跟随功能

### Changed
- 重新开始绘图或导入数据后仅保留垂直光标开关，X-X、Y-Y、观察和统计等临时工具会自动关闭。
- 原始数据 HEX 发送的 CRC 字节顺序由“高低位反转”改为明确选择大端或小端。

### Fixed
- 修复应用信息中的构建时间可能显示不准确的问题。
- 修复众邦 C 配置文件在部分中文注释和仓库示例下识别不稳定的问题。
- 修复通道名编辑后直接点击通道地址时焦点不会正确失去的问题。
- 修复开启最新点跟随后拖动画布不会自动关闭跟随的问题。
- 修复绘图协议发送二进制数据时在原始数据界面可能按文本显示的问题，并标注为绘图发送。
- 修复停止绘图后偶发无法修改通道地址的问题。
- 优化重复点击绘图按钮时的提示显示，避免提示框堆积并缩短状态提示停留时间。
- 优化多通道偏置模式下的右侧刻度列宽，避免长数值挤占相邻通道显示。
- 应用信息页新增高级设置入口，可关闭应用内临时提示信息。

## v1.0.4 - 2026-05-31

### Added
- 新增应用信息入口，显示应用名称、版本和构建时间。
- 新增手动检查更新，并支持启动时自动检查更新开关。
- 更新检查优先使用 GitHub Release，失败后尝试 Gitee Release。

### Changed
- 完善固定帧协议配置，支持帧头、帧尾、CRC 和逐通道数据类型设置。
- 优化固定帧配置下拉框布局和通道编辑弹窗下拉菜单显示。
- 通道列表在无发送协议和 r 协议之间切换时保持更一致的行高和宽度。

### Fixed
- 修复固定帧 CRC 位置、大小端和逐通道类型组合下的解析问题。

## v1.0.3 - 2026-05-31

### Added
- 拆分绘图接收协议和发送协议，新增 r 协议发送支持。
- 支持旧版虚拟示波器 DAT 导入和众邦 C 文件配置识别。

### Changed
- 优化应用字体和绘图工具栏布局。
- 优化随机数据绘图、导入弹窗和串口断线检测。
